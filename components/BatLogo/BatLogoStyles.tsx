import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerLogo: {
    backgroundColor: '#dbd814ff',
    marginTop: 150,
    marginBottom: 50,
    padding: 10,
    borderRadius: '60%',
    borderWidth: 3
  },
  imageLogo: {
    height: 200,
    resizeMode: 'contain'
  }
});